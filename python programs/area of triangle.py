base = input('enter the base of the triangle: ')
height = input('enter the height of the triangle: ')
intermediate = float(base) * float(height)
area = intermediate/2
print('area of the triangle is:',area)
